name: glow_domain
description: Domain layer entities and business rules for Glow
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  glow_core:
    path: ../glow_core
  equatable: ^2.0.5
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.31.0
  build_runner: ^2.13.1
  freezed: ^3.2.5
  json_serializable: ^6.13.1
